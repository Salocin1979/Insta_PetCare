<div>
  <% if user_signed_in? %>
    <header class="user-header">
      <div class="info">
        <h1>Calling a veterinarian</h1>
        <p>Please fill out the form</p>
      </div>
    </header>
    <main>
      <%= simple_form_for [@animal, @user, @consultation] do |f| %>
        <div class= "popular section-wrapper">
          <%= f.association :animal, selected: @animal %>
          <%# <%= f.association :user, :collection => User.where(is_veterinarian: true), label: "Available vet", label_method: :first_name %>
        </div>
      <div class= "popular section-wrapper">
      <p> Available Vets</p>
      <%# <% @users.each do |user| %> 
      <%if current_user.photo.attached?%>
         <%= cl_image_tag current_user.photo.key, height: 150, width: 200, crop: :fill %>
      <% end %>
      <%# <% end %>
      </div>
      <div>
        <%= f.submit "Submit", class: "btn btn-success"%>
        </div>
      <% end %>
    <% else %>
      <div class="categories section-wrapper">
        <div class="flex-header">
          <%= link_to "Please Log In", new_user_session_path%></a>
        </div>
      </div>
    </main>
  <% end %>
</div>
</div>
